# 安装包后默认含 PyYAML（用于 etf_config.yaml）。
# 仅拷贝源码运行时: pip install pyyaml
# Web：pip install fastapi "uvicorn[standard]"
# 日线：pip install akshare
# OpenAI：pip install openai
# 或：pip install -e ".[web]"  /  ".[akshare]"  /  ".[llm]"
